var fs = require('fs');
var path = require('path')
var sequest = require('..');

var key = fs.readFileSync(path.join(process.env.HOME, '.ssh', 'id_rsa'));

var seq = sequest('root@10.112.33.94', {
  privateKey: key,
  command: 'ls',
  proxy: 'root@72.2.114.207',
  passphrase: 'shaarawi.4433'
});
seq.pipe(process.stdout);
seq.write('ifconfig');
seq.write('ls -la');

setTimeout(function () {
  seq.end()
}, 5000);
